<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Beloved Partner To Be is Here </title>
    <%- include('partials/style') %>
</head>
<body>
    <%- include('./partials/navbarUser') %>
    <div class="container mx-auto">
        <h3 > <%= res.hello()%> </h3>
        <a href="/users/recomended" class="btn btn-primary my-1 mx-1"> PetHub's Recommended </a> <br>
        <a href="/users/userpicked" class="btn btn-primary mx-1 my-1" > Your Picks </a>
        <table class="table table-striped table-light">
            <tr>
                <td>Partner ID</td>
                <td>Partner Name</td>
                <td>Partner Type</td>
                <td>Gender</td>
                <td>Actions</td>
            </tr>
            <% data.forEach(element=>{ %>
                <tr>
                    <td> <%= element.id %> </td>
                    <td> <%= element.name %> </td>      
                    <td> <%= element.type %> </td>
                    <td> <%= element.gender %> </td>
                    <td> <a href="/users/petList/<%=element.id %>"> See More  </a>
                    </td>
                </tr>
            <% })   %>
        </table>

    </div>
</body>
</html>